# 1교시
## 컴퓨터(컴퓨트)의 기원은 계산기다 그래서 수학이 연관된다.
- 체력 겸험치 등 감산, 사칙연산 등 필요
- 방어구, 무기 등
- 스킬 고정데미지 장비스텍 등

## 분수
- 위 분자 (3)
- 아래 분모 (5)
- 컴퓨터에서는 (3/5)로 표현
- 통분 : 분모가 다르면 계산할수 없다 
- 최소 공배수 : 분모끼리 분자끼리 곱하면 값이 나옴 분모는 같은 값이로 나오게
- 최대 공약수 : 역수를 해주면 됨

## 넓이와 부피
- 주로 공격 범위(히트박스) 등에쓰인다

## 평균과 백분율(산술 평균)(로그라이크?)
- 확율
- 시간대별 접속자 등에 의한 조절도 한다..
- 가중치를 두기도 한다..
- 지역(이지역의 레벨대에 많이 하냐) - 레벨이 높으면 손쉽게 레벨이 낮으면 버겁게 조절
---
- 평균: 모든 수의 합 / 더한 숫자의 개수
- 평균의 필요한 레벨 디자인 벨런싱 구간에 어느수준에서 오는지 등
---
- 백분율: 통계에서도 쓰이지만 확율에서 많이 쓰임
- 업적 시스템, 진행도 등
---
- 퍼센트의 원리: 현재값 / 전체값 * 비율의 최대 값(100)-백분율에서 %는 100을 의미? 하기 때문
---
## 넓이와 부피
- 물리법칙
- 게임 상에서도 물리 적용이 필요 때렸는데 경직이 없거나 밀리는게없으면 이상하다?


- 2차원에서는 넢이 길이
- 3차원에서는 넓이 길이 부피 까지 있다
- 넓이 : 너비 * 높이(2차원)
- 부피 : 넓이 * 깊이(3차원)

- 원뿔의 부피 공식 구글링해서 찾기만 하면(범위 휩스는공격 같은)
- 블럭쳐진 공식만 알면 된다 (증명은 수학자들이)
- 2변의 길이만 알면 거리를 구할 수 있고 그 거리를 범위 안에 있는지 없는지 판단 타격 가능한지 판단

# 2교시
## 삼각함수
- 몇도로 점프하는지 수류탄 던지는지 대각선으로 때리던지 회전 시키던지 돌리거나 던지거나 그럴경우 사용한다
- 흔들거리는, 바람 등 어떤식으로 하냐
- 이걸 써서 게임이 적용

- 사인은 (높이다)가로길이 구한다(높이/빗변)
- 코사인은 (가로다)세로길이 구한다(밑변/빗변)
- 각도를 넣어주면 사이과 코사인을 자동으로 계산해 줄 것이다.

## 벡터
- 삼각함수에서는 각도?를 다룸
- 벡터는 방향과 길이(피타고라스 점목 그외도 사용)
- 특정 물체의 움직임을 표현하거나 힘의 작용 등을 표현
- 몬스터가 이동하는 방향에서 이동중에 플레이어가 때렸을때 몬스터는 어디로 가냐 그때 그냥 덧셈해주면 된다
- 같은 방향으로 가는 몬스터를 가격했을때 가속을 붙이거나 반대 반향 키를 누르면 브레이트를(f1 게임) 작도한다던지 그럼 그냥 더해주면 됨
- 원리를 이해!!

---
### 심화학습
#### 벡터의 곱셈
https://blog.naver.com/sallygarden_ee/221265467087
#### 내적
- 내적은 벡터의 특정 방향, 성분, 투영(사영)의 크기, 일의 크기, 전류 밀도에 대한 전류의 크기 등을 구할 때 필요
- 간단히 말하면 임의의 벡터의 특정 방향을 가진 성분의 크기를 알아내는데 유용하다는 것
- 내적은 교환법칙이 성립한다.
#### 외적
- 면 벡터의 표현, 토크, 각속도등을 구할때 필요
- 두 벡터에 수직인 벡터를 구한다던지, 한 평면의 법선벡터(수직인벡터)를 구할 때 유용
- 외적은 내적과는 다르게 3차원 공간에서 정의
- 외적은 교환법칙이 성립하지 않는다. 대신, 방향이 반대로 된다.
---

# 3교시 - 컴퓨터 그래픽 (TODO: 해당 부분은 지금 보기에 너무 이해가 어려워 조금 익숙해 지고 다시 보자)
- 그패픽카드는 픽셀로 표현하기 적합(병렬처리 특화)
- 그래픽을 저사양 이지만 많은 양을 처리하기위해 그래픽 카드가 필요 이걸 쿠다코어? 라고한다.
- cpu는 고성능에 적은 양을 처리(직렬처리 특화)
- 랜더링 파이프 라인(입력조립 - 정점셰이더 - 레스터라이저 - 픽셀셰이더 - 출력병합) 단계
---
- cpu는 빠르고  gpu는 느리다, 커맨드 큐를 만든다(드로우콜은 주문을 넣는 행위를하는 역할)(버퍼큐같은 역활인듯)  그 후 GPU 가 처리한다
- 키보드, 모니터 출력 장치??
---
## 정점셰이더(조사필요)
- 도형을 화면 에 배치하기 위해 정정 쉐이더 정한다
- 카메라 보여주고 이런것 결정하는거 같음.. (정확하지 않음)
- 행렬 곱셈 결합 법칙
---
## 레스터 라이져
- 래스터장치는 픽셀을 통해 그래픽을 출력하는 장치
- 색상없음
- 도형들을 점으로 변경해야한다. 픽셀화라고 한다. 이 과정을 레스터 라이져라고한다
---
## 픽셀셰이더
### 쉐이더의 종류
https://blog.naver.com/rja1104/220553716346
- 버텍스 쉐이더, 정점 쉐이더
- 픽셀 쉐이더, PS
- 지오메트리 쉐이더
### 특징 - 픽셀 쉐이더
https://woo-dev.tistory.com/226
- 멀어지거나 가까워 졌을때 표현(질감표현)
- 경계에서 안보이는걸 잘라주는 역활.
- 픽셀 셰이더의 결과물은 색상이다.
- 입력으로 받은 데이터를 통해 하나의 픽셀에 대한 최종적인 색상을 결정하여 반환 
- 게임에선 주로 램버시안 모뎅을 이용한데
- 텍스쳐 - 빛의 정도
- 소묘 역활을 한다
### Diffuse reflection (난반사)
- 난반사광은 모든 뱡향을 따라 같은 강도로 퍼지기 때문이다.
### Specular reflection (정반사)
- 입사각과 반사각은 동일
- 카메라고 들어오는 빛의 세기는 반사광 벡터 r과 카메라 벡터 v의 각에 대한 cos 값의 거듭 제곱 형태
---
## 출력 병합
- 어두워 지면 파랑색 밝아 지면 붉은색
### 후면컬링
https://usingsystem.tistory.com/487?category=1211962
- 카메라를 향하지 않는 즉 화면에 보이지 않는 뒷면을 렌더링 하지 않고 건너뛰는 것
### 뷰표트 변환
https://velog.io/@tjswodud/그래픽스-7.-시점-Viewing
- 3차원 위치 (location)와 3차원 세계 상의 2차원 시점 위치 
- 사이의 이동임. 이러한 3차원->2차원 매핑을 시점 변환
- 장면의 각 물체의 이미지 공간 위치를 빠르게 찾아야 하는 객체 순서 렌더링에서 중요한 역할
### 스캔 변환
https://jy05un.tistory.com/15
- 아날로그 형태의 어떠한 도형을 디지털 형태의 도형으로 바꿔주는 프로세스
- 스캔 변환이라는 기술이 필요하다 픽셀이다 보니 너무 경계가 뚜렷해 이는 해결하기 위한 기술로 안티얼라이언싱이라는 기법을 사용한다.
#### 그래픽 도형 선분로서의 조건
- 양 끝점을 지난 직선이면서 끝이 있어야 함
- 주어진 점의 순서와 무관하게 정의되어야 함
- 기울기와 상관없이 시각적으로 동일한 밝기를 가져야 함
### 클리핑
https://velog.io/@jm450_/그래픽스-클리핑-Clipping
- 윈도우 밖은 그릴 필요가 없다. 실질적으로 보이는 부분만 처리해준다

